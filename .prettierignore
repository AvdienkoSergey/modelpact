node_modules
package-lock.json
# Build output: tsc decides its own line breaks, and they are not ours to check.
dist/
# Generated by release-please; reformatting it fights the tool.
CHANGELOG.md
# Vendored MDN copies — kept byte-identical to upstream so diffs stay readable.
docs/
# Patch payloads: reformatting one breaks the patch it applies.
patches/
