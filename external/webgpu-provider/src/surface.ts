/**
 * Everything the package offers, named once.
 *
 * `tsconfig.surface.json` compiles this file and nothing else, with
 * `skipLibCheck` off, so the published declarations are checked as a consumer
 * receives them. A type that only compiles inside the package fails here.
 */

import {
  AiError,
  contextUsage,
  createProvider,
  defineProviders,
  err,
  failureFromError,
  findProviderName,
  fraction,
  jsonSchema,
  makeMockProvider,
  makeOllamaProvider,
  makePromptApiProvider,
  ndjsonLines,
  ok,
  tokens,
  type AccessKind,
  type AiFailure,
  type AiMessage,
  type AiProvider,
  type AiRole,
  type Availability,
  type ConnectOptions,
  type ContextUsage,
  type FailureKind,
  type Fraction,
  type GenerateOptions,
  type GenerateRequest,
  type JsonSchema,
  type Modality,
  type ModalityExpectation,
  type ModelAccess,
  type ModelBackend,
  type ModelConnection,
  type ModelRequest,
  type MockConfig,
  type OllamaConfig,
  type ProviderName,
  type ProviderRegistry,
  type Result,
  type SessionOptions,
  type Tokens,
  type UsageKind,
} from "modelpact";
import { CONTRACT_SCHEMA, describeContract } from "modelpact/testing";
import {
  createProvider as createProviderFromDoor,
  findTool,
  runTool,
  toolThrewFailure,
  type ModelBackend as ModelBackendFromDoor,
  type Tool,
} from "modelpact/backend";

export const values = {
  AiError,
  contextUsage,
  createProvider,
  defineProviders,
  err,
  failureFromError,
  findProviderName,
  fraction,
  jsonSchema,
  makeMockProvider,
  makeOllamaProvider,
  makePromptApiProvider,
  ndjsonLines,
  ok,
  tokens,
  CONTRACT_SCHEMA,
  describeContract,
  createProviderFromDoor,
  findTool,
  runTool,
  toolThrewFailure,
};

export type DoorTypes = [ModelBackendFromDoor, Tool];

export type Types = [
  AccessKind,
  AiFailure,
  AiMessage,
  AiProvider,
  AiRole,
  Availability,
  ConnectOptions,
  ContextUsage,
  FailureKind,
  Fraction,
  GenerateOptions,
  GenerateRequest,
  JsonSchema,
  Modality,
  ModalityExpectation,
  ModelAccess,
  ModelBackend,
  ModelConnection,
  ModelRequest,
  MockConfig,
  OllamaConfig,
  ProviderName,
  ProviderRegistry,
  Result<string, AiFailure>,
  SessionOptions,
  Tokens,
  UsageKind,
];
